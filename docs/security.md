# Security

Skill execution may run arbitrary local scripts, so the runtime needs clear boundaries.

- Restrict script execution to approved skill packages.
- Trace execution for auditability.
- Prefer sandboxed execution for untrusted skills.
