# Skill Format

This repository centers on standard `SKILL.md` packages.

- A skill package contains one `SKILL.md` file plus optional references and scripts.
- The runtime should scan skill roots, parse metadata, and build an execution graph.
- The runtime should load only the resources needed for a given request.
