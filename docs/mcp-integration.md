# MCP Integration

The MCP server should expose skill discovery, inspection, and execution-oriented tools.

Initial scaffold behavior:

- `GET /health` returns a basic health response.
- Root requests return a minimal JSON scaffold response.
